<Window
    x:Class="Sharp3DRbxAvatar.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:helix="http://helix-toolkit.org/wpf"
    Title="3D Viewer"
    Width="350"
    Height="550"
    Background="Black"
    ResizeMode="CanResizeWithGrip"
    Topmost="True">
    <Window.Resources>
        <Style x:Key="NavButton" TargetType="Button">
            <Setter Property="Background" Value="#1a1a2e" />
            <Setter Property="Foreground" Value="#e0e0e0" />
            <Setter Property="BorderBrush" Value="#2a2a4a" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="6,2" />
            <Setter Property="MinWidth" Value="28" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#2a2a5e" />
                                <Setter TargetName="border" Property="BorderBrush" Value="#5050aa" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3a3a7e" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.35" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  User selector  -->
        <Border
            Grid.Row="0"
            Margin="4,4,4,0"
            Padding="4"
            Background="#0a0a0a"
            BorderBrush="#292929"
            BorderThickness="1"
            CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    x:Name="BtnPrevUser"
                    Grid.Column="0"
                    Click="BtnPrevUser_Click"
                    Content="◀"
                    Style="{StaticResource NavButton}" />
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <TextBlock
                        x:Name="TxtUserLabel"
                        HorizontalAlignment="Center"
                        FontSize="8"
                        Foreground="#666680"
                        Text="USER" />
                    <TextBlock
                        x:Name="TxtUserName"
                        HorizontalAlignment="Center"
                        FontSize="11"
                        FontWeight="SemiBold"
                        Foreground="#c0c0e0"
                        Text="—" />
                </StackPanel>
                <Button
                    x:Name="BtnNextUser"
                    Grid.Column="2"
                    Click="BtnNextUser_Click"
                    Content="▶"
                    Style="{StaticResource NavButton}" />
            </Grid>
        </Border>

        <!--  3D Viewport  -->
        <Grid
            x:Name="DisplayGrid"
            Grid.Row="1"
            Margin="4,4,4,0" />

        <!--  Version selector  -->
        <Border
            Grid.Row="2"
            Margin="4"
            Padding="4"
            Background="#0a0a0a"
            BorderBrush="#292929"
            BorderThickness="1"
            CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button
                    x:Name="BtnPrevVersion"
                    Grid.Column="0"
                    Click="BtnPrevVersion_Click"
                    Content="◀"
                    Style="{StaticResource NavButton}" />
                <StackPanel
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                    <TextBlock
                        x:Name="TxtVersionLabel"
                        HorizontalAlignment="Center"
                        FontSize="8"
                        Foreground="#666680"
                        Text="AVATAR VERSION" />
                    <TextBlock
                        x:Name="TxtVersionInfo"
                        HorizontalAlignment="Center"
                        FontSize="11"
                        FontWeight="SemiBold"
                        Foreground="#c0c0e0"
                        Text="—" />
                </StackPanel>
                <Button
                    x:Name="BtnNextVersion"
                    Grid.Column="2"
                    Click="BtnNextVersion_Click"
                    Content="▶"
                    Style="{StaticResource NavButton}" />
            </Grid>
        </Border>
        <!--  Download by Player ID  -->
        <Border
            Grid.Row="3"
            Margin="4,0,4,4"
            Padding="4"
            Background="#0a0a0a"
            BorderBrush="#292929"
            BorderThickness="1"
            CornerRadius="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TextBox
                    x:Name="TxtDownloadId"
                    Grid.Row="0"
                    Grid.Column="0"
                    Margin="0,0,4,0"
                    Padding="4,2"
                    Background="#1a1a2e"
                    BorderBrush="#2a2a4a"
                    BorderThickness="1"
                    FontSize="11"
                    Foreground="#c0c0e0" />
                <Button
                    x:Name="BtnDownload"
                    Grid.Row="0"
                    Grid.Column="1"
                    Click="BtnDownload_Click"
                    Content="Download"
                    Style="{StaticResource NavButton}" />
                <TextBlock
                    x:Name="TxtDownloadStatus"
                    Grid.Row="1"
                    Grid.Column="0"
                    Grid.ColumnSpan="2"
                    Margin="2,2,0,0"
                    FontSize="8"
                    Foreground="#666680"
                    Text="Enter a Player ID and click Download" />
            </Grid>
        </Border>
    </Grid>
</Window>
